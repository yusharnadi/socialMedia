import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  title: {
    fontFamily: 'Inter',
    fontSize: 24,
    fontWeight: '600',
    color: '#022150',
  },
});
export default style;
